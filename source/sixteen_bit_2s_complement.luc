module sixteen_bit_2s_complement (
    input a[16], // 16 bit input to be negated using 2's complement
    output n[16] //negated value
  ) {
sig temp;
  
  always {
    temp = ~a; //not for each bit
    n = temp+1; //add 1 to ~x
  }
}
